# Legal Work Space 当前交接说明

> 更新时间：2026-08-11（Asia/Shanghai）  
> 项目目录：`/Users/yushancheng/Desktop/工作台搭建/lawyer-workbench`  
> 当前版本：`1.0.0`
> 状态：v1.0.0 正式发布基线

## 1. 给接手 Agent 的第一条指令

请先阅读本文件，然后在项目目录执行只读检查：

```bash
git status --short
git diff --stat
npm run check
```

重要约束：

- 工作区在本轮开始前就已经存在大量用户改动，不能把全部 diff 都视为本轮产生。
- 禁止使用 `git reset --hard`、`git checkout --` 或批量删除未跟踪文件。
- 不要提交、推送、打 tag、生成 Release，除非用户明确授权。
- 修改前先确认目标文件是否同时包含用户原有改动。
- 当前根目录 `/Users/yushancheng/Desktop/工作台搭建` 不是仓库；真正仓库是其下的 `lawyer-workbench`。

## 2. 项目概况

这是一个面向独立律师的本地工作台：React + TypeScript + Vite + Dexie/IndexedDB，并提供 Electron 桌面壳。

- 数据默认只保存在本地 IndexedDB。
- 页面导航使用 `AppContext` 自定义状态路由，不使用 `react-router-dom`。
- Electron 生产环境通过 `file://` 加载 `dist/index.html`，Vite `base` 必须保持 `./`。
- 数据库当前为 Dexie v5，共 19 张表（含旧版兼容表 `timeRecords`、`invoices`）。
- 新计时和案件咨询统计统一使用 `legalConsultations`；旧表保留但界面不混算。

## 3. 本轮已完成的修复

### 3.1 数据备份与恢复

关键文件：

- `src/backup/backupCodec.ts`
- `src/backup/backupService.ts`
- `src/backup/backup.test.ts`
- `src/pages/settings/SettingsPage.tsx`

当前行为：

- 从 `db.tables` 动态导出所有 Dexie 表，不再维护易遗漏的手写名单。
- `Blob` 使用 MIME、大小和 Base64 可逆编码，恢复后仍为 `Blob`。
- 导入前完成格式、版本和数据结构校验。
- 全库恢复在单个 Dexie `rw` 事务中完成；任一表写入失败则全部回滚。
- 未识别的新表会安全忽略并提示。
- 旧备份中已经被 JSON 转成 `{}` 的文件内容无法恢复，只恢复安全元数据并明确警告。
- 清空业务数据覆盖新增表，但保留 `settings`。
- 导入/导出设置 512 MB 安全上限。注意：JSON/Base64 仍是内存内处理，接近上限时内存峰值未做真实压力测试。

### 3.2 法律期限与保全规则

关键文件：

- `src/utils/legalDeadlines.ts`
- `src/utils/legalDeadlines.test.ts`
- `src/pages/cases/CaseDetail.tsx`
- `src/components/preservation/PreservationCard.tsx`

当前行为：

- 切换案件阶段到“立案”或“等待判决”不再自动生成虚假期限。
- 举证截止日由用户依据法院举证通知书录入。
- 上诉辅助计算必须同时输入文书类型和实际送达日期：判决 15 日、可上诉裁定 10 日。
- UI 明确提示节假日顺延、送达效力、涉外规则等仍需人工核对。
- 保全司法解释条号已从旧“第487条”更新为现行“第485条”。
- “其他”保全措施不再默认 6 个月，必须手工录入。
- 辅助日期不作为个案法律结论，最终以法院文书为准。

法源核验日期为 2026-08-10，官方来源：

- 《中华人民共和国民事诉讼法》2023 年修正决定：`https://www.npc.gov.cn/npc/c2/c30834/202309/t20230901_431419.html`
- 《最高人民法院关于民事诉讼证据的若干规定》：`https://www.court.gov.cn/zixun/xiangqing/212721.html`
- 《最高人民法院关于适用〈中华人民共和国民事诉讼法〉的解释》2022 年修正文本：`https://www.court.gov.cn/zixun/xiangqing/353651.html`

如继续改动法律规则，必须重新核验现行效力，不得只凭本文件或模型记忆引用。

### 3.3 法律咨询计时器

关键文件：

- `src/utils/consultationTimer.ts`
- `src/utils/consultationTimerPersistence.ts`
- `src/utils/consultationTimerLifecycle.ts`
- `src/utils/consultationDates.ts`
- `src/store/AppContext.tsx`
- `src/pages/consultation/ConsultationPage.tsx`
- `src/utils/caseConsultations.ts`

当前行为：

- 只有运行状态才累计当前时间段，暂停等待时间不会在停止时重复加入。
- 计时状态持久化到 `localStorage`，支持刷新/重启后恢复。
- 每次会话具有稳定 `timerId`，数据库保存做幂等检查。
- 结束结果为：`saved`、`already-saved`、`too-short`、`failed`、`no-timer`。
- 结束时先冻结并保留暂停快照，等待 IndexedDB 事务；失败时保留会话并允许重试。
- 同一会话并发结束共享一个 Promise，避免双击重复写入。
- 不足 10 秒不保存；其余四舍五入且至少 1 分钟，90 秒为 2 分钟。
- 手工记录、计时记录和日期筛选统一采用本地自然日；“到”日期使用下一本地零点作为排他上界。
- 案件详情、打印汇总和案件 JSON 导出统一读取 `legalConsultations`。

固有限制：操作系统强制杀进程时无法保证正在执行的 IndexedDB 事务完成，但成功前暂停会话仍保留在 `localStorage`，下次可以幂等重试。

### 3.4 打印与 Electron 安全

关键文件：

- `src/utils/printDocuments.ts`
- `src/utils/browserPrint.ts`
- `electron/navigation-policy.cjs`
- `electron/main.cjs`

当前行为：

- 案件报告和常法顾问报告不再依赖 `window.open`。
- 固定打印模板中的所有用户字段统一进行 HTML 转义。
- 通过不可见但可布局的 iframe 打印；`afterprint` 后清理，60 秒无事件时兜底清理并视为打印已启动。
- 无 `contentWindow` 或 `print()` 抛错时立即清理并返回错误。
- Electron 保持 `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`。
- 新窗口始终拒绝；非内部 HTTP(S) 交给系统浏览器。
- 开发环境仅允许与 `DEV_URL` 严格同源的顶层导航。
- 生产环境仅允许规范化后位于 `dist` 内的 `file:` URL；路径逃逸和危险协议默认拒绝。

仍需人工验证：macOS/Windows 实际打印对话框、打包后的 `file://` 顶层导航。

### 3.5 日历、版本与性能

关键文件：

- `src/utils/calendarNavigation.ts`
- `src/pages/calendar/CalendarPage.tsx`
- `src/utils/appVersion.ts`
- `src/appRoutes.tsx`
- `src/App.tsx`

当前行为：

- 日历由单一 `{ view, anchor }` 状态驱动。
- 月视图导航移动自然月，周视图移动自然周，“今天”重置锚点。
- 跨月、跨年周标题显示完整范围。
- 界面版本直接读取 `package.json`，不再维护第二份版本常量。
- 11 个页面使用 `React.lazy` 按需加载，带加载占位和错误边界。
- 生产主入口由约 665.68 kB 降至约 341.73 kB，Vite 大块警告已消失。
- 构建产物保持相对 `./assets/...` 引用，兼容 Electron `file://`。

## 4. 当前验证基线

最后一次完整验证结果：

- `npm run check`：通过。
- Vitest：14 个测试文件，69 项测试全部通过。
- TypeScript `tsc -b`：通过。
- Vite 生产构建：通过，无大于 500 kB 的 chunk 警告。
- `node --check electron/main.cjs`：通过。
- `node --check electron/navigation-policy.cjs`：通过。
- `git diff --check`：通过。
- Headless Chrome 临时 profile 旧基线：9 个主要页面正常懒加载，日历周导航同步，`console.error` 和 `pageerror` 均为 0。本次发布已通过版本单测及安装包元数据核验确认显示版本为 v1.0.0。

注意：`electron/navigation-policy.test.js` 是 **Vitest** 测试，随 `npm test` 运行；不要用 `node --test` 单独运行它。

运行环境可能输出一条 fnm 无法在 `~/.local/state/fnm_multishells` 创建软链接的权限警告。该警告没有影响上述命令的退出码或验证结果。

## 5. 当前 Git/工作区状态

当前所有修复仍未提交。主要已修改文件包括：

- `electron/main.cjs`
- `package.json`、`package-lock.json`
- `src/App.tsx`
- `src/db/database.ts`
- `src/store/AppContext.tsx`
- 设置、日历、案件、保全、常法报告等页面和组件

主要新增文件/目录包括：

- `src/backup/`
- `src/test/`
- `src/appRoutes.tsx` 及测试
- `src/utils/` 下的版本、打印、日历、期限、咨询计时和日期模块及测试
- `electron/navigation-policy.cjs` 及测试
- `vitest.config.ts`

完整列表必须以接手时的 `git status --short` 为准。不要根据本节批量暂存或清理，因为工作区中还混有本轮开始前的用户改动，例如 Dashboard、Billing、Docs、案件文档区及数据库迁移相关变更。

## 6. 尚未完成或需要产品决策的事项

以下不是已确认的回归 bug，不应在没有用户确认的情况下直接实现：

1. 模板中心、富文本编辑器、模板变量自动填充及 Word/PDF 导出。
2. AI 助手：供应商、密钥存储、是否联网、隐私边界、法律检索来源均未确定。
3. 全面响应式/移动端布局改版。
4. `Settings.hourlyRate`、`includeRetainerHours` 与当前“法律咨询 + 用户上传票据”业务的去留和实际用途。
5. 旧 `timeRecords`、`invoices` 数据是否需要正式迁移或只保留兼容。

开始这些事项前，应先提出 2—3 个方案、确认产品选择，再单独开任务实施。

## 7. 建议的下一步顺序

1. 让用户确认本轮 diff 是否可以提交，并决定是否拆分多个 commit。
2. 在真实 Electron 桌面窗口中人工验证：启动、内部导航、案件打印、常法报告打印。
3. 使用小/中/大三档真实备份验证文件往返；接近 512 MB 的测试应单独安排，避免内存压力影响用户环境。
4. 用户选择产品方向后，再依次设计模板中心、AI 助手、响应式布局。
5. 只有用户明确授权后才推送、打 tag 或生成 Release。

## 8. 快速验证命令

```bash
cd /Users/yushancheng/Desktop/工作台搭建/lawyer-workbench
npm run check
npx vitest run electron/navigation-policy.test.js src/utils/browserPrint.test.ts
node --check electron/main.cjs
node --check electron/navigation-policy.cjs
git diff --check
git status --short
```
